#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../webpack-cli-scripts/scripts/opencollective.sh" "$@"
  ret=$?
else 
  bash  "$basedir/../webpack-cli-scripts/scripts/opencollective.sh" "$@"
  ret=$?
fi
exit $ret
